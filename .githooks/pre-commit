#!/bin/sh

set -e

echo "=== pre-commit: checking format ==="
cargo fmt --all --check

echo "=== pre-commit: clippy ==="
cargo clippy --workspace --all-targets -- -D warnings

echo "=== pre-commit: OK ==="
